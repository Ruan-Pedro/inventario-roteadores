import { DbforDirective } from './dbfor.directive';

describe('DbforDirective', () => {
  it('should create an instance', () => {
    const directive = new DbforDirective();
    expect(directive).toBeTruthy();
  });
});
